// import { isDev } from '../../configs/env.js';

// export const responseFailed = ({ res, error, message = error.message || 'Sorry, Something went wrong.', status = 500 }) => {
// 	return res.status(statusCode).json({ success: false, message, error: isDev ? error : undefined });
// };
